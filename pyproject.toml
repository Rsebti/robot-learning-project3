[project]
name = "robot-learning-project3"
version = "0.1.0"
description = "Robot Learning Project 3 — Eval 2 RL on SO-101"
requires-python = ">=3.10"
dependencies = []

[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
include = ["sim*"]

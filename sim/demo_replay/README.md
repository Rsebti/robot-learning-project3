# Demo replay (legacy)

**Handoff for your teammate:** use **[`RLPD/`](../../RLPD/README.md)** instead.

- Pre-extracted annotations: `RLPD/data/episodes.json`
- Isaac replay / verify: `RLPD/scripts/`

This package (`sim/demo_replay/`) still contains the **FK annotation** pipeline (`annotate_grasp_fk.py`) for re-running extraction on Hugo’s machine. Friends doing RLPD in sim do not need those scripts if `RLPD/data/` is already populated.
